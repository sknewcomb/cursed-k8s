apiVersion: v1
kind: ConfigMap
metadata:
  name: header-leak-config
  namespace: header-leak
data:
  ENVIRONMENT: "production"
  APP_VERSION: "2.1.0"
  INTERNAL_TOKEN: "internal-secret-token-xyz789"
  DATABASE_PATH: "/var/lib/app/database.sqlite"
  ADMIN_TOKEN: "admin-token-abc123"
  # The flag is in an environment variable that gets exposed via headers!
  FLAG: "FLAG{headers_are_public_metadata}"

